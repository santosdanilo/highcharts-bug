import { ProjectionDirective } from './projection.directive';

describe('ProjectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectionDirective(null);
    expect(directive).toBeTruthy();
  });
});
